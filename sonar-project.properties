sonar.projectKey=poc-agent4-replit
sonar.projectName=PoC Agent4 Replit
sonar.projectVersion=0.1.0

sonar.sources=core,agents
sonar.tests=tests
sonar.python.version=3.11

sonar.host.url=http://localhost:9000

# sonar.token must NOT be stored here.
# Set SONAR_TOKEN env var or add it to .env (which is gitignored).
# e.g.:  export SONAR_TOKEN=squ_...
#        or add SONAR_TOKEN=squ_... to .env

sonar.sourceEncoding=UTF-8
sonar.exclusions=**/__pycache__/**,**/*.pyc,workspace/agent_sandbox/**,.venv/**,build/**,*.egg-info/**
